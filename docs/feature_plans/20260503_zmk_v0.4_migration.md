# ZMK v0.4 Migration Plan (Zephyr 4.1 / HWMv2)

**Date:** 2026-05-03  
**Status:** Planning  
**Current pinned version:** v0.3 (Zephyr 3.5)  
**Target:** ZMK v0.4 / `main` (Zephyr 4.1, HWMv2)

## Background

ZMK `main` moved to Zephyr 4.1 in December 2025, introducing Hardware Model v2 (HWMv2) — a fundamental restructuring of how boards are defined. All out-of-tree board definitions must be migrated. Shields do **not** require HWMv2 changes but have other breaking changes (Cirque driver, Kconfig removals, etc.).

This migration spans **four repositories**:
- `zmk-fingerpunch-keyboards` (this repo)
- `zmk-fingerpunch-controllers`
- `zmk-fingerpunch-vik`
- `zmk-config-fp-example` (user-facing example/template repo)

**Note:** Hundreds of users have forked or followed `zmk-config-fp-example` to create their own `zmk-config` repos. Every breaking change in the upstream repos has a downstream ripple effect on those users. This plan must account for user-facing communication and migration guidance.

**Reference:** https://zmk.dev/blog/2025/12/09/zephyr-4-1

---

## Repositories and Components Summary

### zmk-fingerpunch-keyboards — Boards (`boards/arm/`)

| Board | SoC | Revisions | Split | Notes |
|---|---|---|---|---|
| `ffkb_holyiot_v1` | nRF52840 | None | No | Integrated keyboard, has DCDC Kconfig |
| `pinkies_out_v3` | RP2040 | 3.0.0, 3.1.0 | No | VIK connector |
| `vulpes_majora_v1` | RP2040 | None | No | VIK connector, shift register |
| `ximi` | RP2040 | 1.0.0, 2.0.0 | Yes (left/right) | VIK connector (per-revision), SplitKB Halcyon physical footprint, **manual migration required** |

### zmk-fingerpunch-keyboards — Shields (`boards/shields/`)

| Shield | Type | Cirque Usage | Per-Board Overlays |
|---|---|---|---|
| `arachnophobe` | Single | No | `seeeduino_xiao.conf`, `seeeduino_xiao_ble.conf`, `seeeduino_xiao_rp2040.conf` |
| `bykeeb` | Split (L/R) | No | `seeeduino_xiao.conf`, `seeeduino_xiao_ble.conf`, `seeeduino_xiao_rp2040.conf` |
| `ffkb_v1` | Single | No | `nice_nano.overlay`, `nice_nano_v2.*` |
| `ffkb_v2` | Single | Yes (`dr-gpios`, `no-taps`) | `nice_nano.overlay`, `nice_nano_v2.*` |
| `ffkb_v3` | Single | Yes (via VIK — uses `vik_cirque_i2c`/`vik_cirque_spi` shields) | `nice_nano_v2.overlay`, `svlinky.overlay`, `vikoto.overlay` |
| `ffkb_lite_v1` | Single | No | None |
| `ffkb_we_v1` | Single | Yes (`dr-gpios`, `no-taps`) | None |
| `helmet_head` | Single | No | None |
| `kucheza_byomcu` | Single | No | None |
| `pinkies_out_v1` | Single | No | `nice_nano.overlay`, `nice_nano_v2.overlay` |
| `pinkies_out_v2` | Single | Yes (`dr-gpios`, `no-taps`) | `nice_nano_v2.keymap`, `nice_nano_v2.overlay` |
| `rock_on_v1` | Single | No | `nice_nano.overlay`, `nice_nano_v2.overlay` |
| `swweeep` | Split (L/R) | No | None |
| `vulpes_minora` | Split (L/R) | No | `nice_nano.overlay`, `nice_nano_v2.*` |

### zmk-fingerpunch-controllers — Boards (`boards/arm/`)

| Board | SoC | Revisions | Notes |
|---|---|---|---|
| `vikoto` | nRF52840 | None | DCDC Kconfig, pinmux.c, Pro Micro pins |
| `svlinky` | RP2040 | 0.1.0, 0.2.0 | VIK connector |
| `xivik` | RP2040 | 0.1.0, 0.2.0, 0.3.0 | VIK connector, XIAO connector |

### zmk-fingerpunch-vik — Shields

| Shield | Cirque Usage | Per-Board Overlays |
|---|---|---|
| `vik_cirque_i2c` | Yes (`dr-gpios`, `no-taps`, `cirque,pinnacle`) | `sadekbaroudi_svlinky.overlay`, `sadekbaroudi_xivik.overlay`, `vikoto.overlay` |
| `vik_cirque_spi` | Yes (`dr-gpios`, `no-taps`, `cirque,pinnacle`) | None |
| `vik_cirque_spi_split` | Yes (`dr-gpios`, `no-taps`, `cirque,pinnacle`) | None |
| `vik_pmw3360_per56` | No | None |
| `vik_pmw3610` | No | None |
| `vik_vikropad` | No | None |

### zmk-config-fp-example — User Config Template

This is the **template/example repo** that users fork or follow to create their own `zmk-config` repos. It directly demonstrates how to use fingerpunch keyboards with custom keymaps and optional Cirque trackpad configuration.

| File | Issues Found |
|---|---|
| `config/west.yml` | Points to `petejohanson/zmk@feat/pointers-move-scroll` (stale, 447+ commits behind, 2+ years old). Also imports `zmk-fingerpunch-keyboards` which chains to controllers+vik. |
| `build.yaml` | Uses `nice_nano_v2` board name → must become `nice_nano//zmk` |
| `config/ffkb_v2.overlay` | Uses Cirque out-of-tree properties: `compatible = "cirque,pinnacle"`, `dr-gpios`, `sleep`, `no-taps`. Also uses `zmk,input-listener` properties `xy-swap`, `y-invert`, `x-invert` (these are ZMK properties, NOT Cirque driver properties — verify they're unchanged). |
| `config/ffkb_v2.conf` | `CONFIG_I2C=y` — verify still needed or auto-enabled |
| `.github/workflows/build.yml` | Uses `@main` — should pin to `@v0.4` for stability |
| `README.md` | References Pete's `cirque-input-module` and `feat/pointers-move-scroll` branch — both stale |
| `zephyr/module.yml` | `board_root: .` — verify still correct after HWMv2 |

**Critical downstream impact:** Every user who followed this template has similar files in their own repos. The migration guide must help them update their own configs.

---

## Breaking Changes Requiring Action

### 1. HWMv2 Board Migration (ALL boards across keyboards + controllers repos)

**What changed:** Boards no longer live under `boards/arm/`. They must be placed in a vendor-named directory (e.g., `boards/fingerpunch/`) and require a `board.yml` manifest file.

**Required for each board:**
- Move from `boards/arm/<board>/` → `boards/fingerpunch/<board>/`
- Create `board.yml` with board name, vendor, SoC, revisions, and optional variants
- Rename `Kconfig.board` → `Kconfig.<board>`
- In `Kconfig.<board>`: Remove `bool` line, change `depends on` → `select`
- In `<board>_defconfig`: Remove `CONFIG_SOC_SERIES_*`, `CONFIG_SOC_NRF*`/`CONFIG_SOC_RP2040`, `CONFIG_BOARD_*`
- Add ZMK board variant support (add `zmk` variant to `board.yml` socs)

**Note:** The Zephyr `board_v1_to_v2.py` script does NOT work well with split boards — **ximi must be migrated manually**.

### 2. nRF52840 DC/DC Mode (ffkb_holyiot_v1, vikoto)

**What changed:** DC/DC mode is now configured in devicetree, not Kconfig.

- Remove `BOARD_ENABLE_DCDC` / `SOC_DCDC_NRF52X` from `Kconfig` and `Kconfig.defconfig`
- Add `&reg1 { regulator-initial-mode = <NRF5X_REG_MODE_DCDC>; };` to board `.dts`

### 3. nRF52840 NFC Pins as GPIO

**What changed:** `CONFIG_NFCT_PINS_AS_GPIOS` Kconfig removed; use devicetree instead.

- If any board/shield uses NFC pins as GPIO, add `&uicr { nfct-pins-as-gpios; };` to devicetree
- Remove any `CONFIG_NFCT_PINS_AS_GPIOS=y` from Kconfig/defconfig (none found currently, but verify)

### 4. RP2040 Devicetree Changes (pinkies_out_v3, vulpes_majora_v1, ximi, svlinky, xivik)

**What changed:** RP2040 include path changed, fixed-clock node removed, clock control required.

- Change `#include <rpi_pico/rp2040.dtsi>` → `#include <raspberrypi/rpi_pico/rp2040.dtsi>`
- Remove `xtal-clk` / `fixed-clock` node from DTS
- Add `CONFIG_CLOCK_CONTROL=y` to defconfig
- Add `&timer { status = "okay"; };` and `&rtc` / `&vreg` nodes to DTS
- Add `zephyr,flash-controller = &ssi;` to chosen node

### 5. Cirque Pinnacle Driver Migration (zmk-fingerpunch-vik + shields in keyboards)

**What changed:** Upstream Zephyr now includes the Pinnacle driver. The `petejohanson/cirque-input-module` module must be removed.

**Property renames:**
| Old (out-of-tree) | New (upstream Zephyr) |
|---|---|
| `dr-gpios` | `data-ready-gpios` |
| `no-taps` | *(remove; use `primary-tap-enable` to opt in)* |
| `x-invert` / `y-invert` | `invert-x` / `invert-y` |
| `rotate-90` | `swap-xy` |
| `sleep` | `sleep-mode-enable` |

**Files affected:**
- `zmk-fingerpunch-keyboards`: `ffkb_v2.overlay`, `ffkb_we_v1.overlay`, `pinkies_out_v2.overlay`
- `zmk-fingerpunch-vik`: `vik_cirque_i2c.overlay`, `vik_cirque_spi.overlay`, `vik_cirque_spi_split_peripheral.overlay`
- `zmk-config-fp-example`: `config/ffkb_v2.overlay` (user-facing example with `dr-gpios`, `sleep`, `no-taps`, and `sensitivity` properties)

**Note on `zmk,input-listener` properties:** The properties `xy-swap`, `y-invert`, `x-invert` used on `zmk,input-listener` nodes (e.g., in `zmk-config-fp-example`) are ZMK-specific, NOT Cirque driver properties. These should be **unchanged** in ZMK v0.4 but verify.

**Note on `compatible` string:** Verify whether `compatible = "cirque,pinnacle"` remains valid for both I2C and SPI, or if it needs to change to `cirque,pinnacle-i2c` / `cirque,pinnacle-spi`.

**Note on `sensitivity` property:** The `zmk-config-fp-example` uses `sensitivity = "4x"` — verify this property exists in the upstream Zephyr Pinnacle driver.

**Module removal:**
- Remove `cirque-input-module` from `zmk-fingerpunch-vik/config/deps.yml`
- Users who manually added `cirque-input-module` to their own `west.yml` (following old zmk-config-fp-example README) must also remove it

### 6. CONFIG_WS2812_STRIP Removal

**What changed:** Kconfig flag removed; SPI is auto-enabled.

- Remove `CONFIG_WS2812_STRIP=y` from:
  - `ffkb_holyiot_v1_defconfig`
  - `pinkies_out_v3_defconfig`
  - `vulpes_majora_v1_defconfig`

### 7. ZMK In-Tree Board Name Changes (build.yaml + shield overlay directories)

**What changed:** ZMK in-tree boards were renamed/reorganized.

| Old Name | New Name (shorthand) |
|---|---|
| `seeeduino_xiao_ble` | `xiao_ble//zmk` |
| `seeeduino_xiao_rp2040` | `xiao_rp2040//zmk` |
| `nice_nano_v2` | `nice_nano//zmk` |
| `nice_nano` (v1) | `nice_nano@1//zmk` |

**Impact on build.yaml:** Every `board:` entry referencing these must be updated.

**Impact on shield per-board overlays:** Files in `boards/shields/*/boards/` named after these boards must be renamed:
- `nice_nano_v2.overlay` → `nice_nano.overlay` (since nice_nano defaults to v2)
- `nice_nano_v2.conf` → `nice_nano.conf`
- `nice_nano_v2.keymap` → `nice_nano.keymap`
- `seeeduino_xiao_ble.conf` → `xiao_ble.conf`
- `seeeduino_xiao_rp2040.conf` → `xiao_rp2040.conf`
- `seeeduino_xiao.conf` → may need removal or renaming

**Note:** Existing `nice_nano.overlay` files (for v1) will conflict — need to be renamed to `nice_nano@1.overlay` to distinguish from the now-default v2. Per ZMK blog: v1 = `nice_nano@1.0.0` (shorthand `nice_nano@1`), v2 = `nice_nano@2.0.0` (shorthand `nice_nano`, the default). The per-board overlay file should use the shorthand: `nice_nano@1.overlay`.

**Affected shield directories with per-board overlays:**
- `arachnophobe/boards/` — seeeduino_xiao files
- `bykeeb/boards/` — seeeduino_xiao files
- `ffkb_v1/boards/` — nice_nano + nice_nano_v2 files
- `ffkb_v2/boards/` — nice_nano + nice_nano_v2 files
- `ffkb_v3/boards/` — nice_nano_v2, svlinky, vikoto files
- `pinkies_out_v1/boards/` — nice_nano + nice_nano_v2 files
- `pinkies_out_v2/boards/` — nice_nano_v2 files
- `rock_on_v1/boards/` — nice_nano + nice_nano_v2 files
- `vulpes_minora/boards/` — nice_nano + nice_nano_v2 files
- `vik_cirque_i2c/boards/` (vik repo) — svlinky, xivik, vikoto files

### 8. CONFIG_ZMK_BLE_EXPERIMENTAL_CONN / CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES

**Status:** Still present in ZMK v0.3 Kconfig (not removed). Verify if still supported in v0.4 / Zephyr 4.1. If removed, delete from all `.conf` files (15+ files affected).

### 9. West Manifest & Dependency Cleanup

**zmk-fingerpunch-keyboards (`config/west.yml`):**
- Update `revision: v0.3` → `revision: v0.4` (or `main` temporarily)
- Remove `petejohanson` remote if no longer needed (chiptuner-zmk-module)
- Verify `chiptuner-zmk-module` compatibility with Zephyr 4.1

**zmk-fingerpunch-controllers (`config/west.yml`):**
- Replace `petejohanson/zmk@feat/pointers-move-scroll` → `zmkfirmware/zmk@v0.4`
- This branch is 447 commits behind and 2+ years stale

**zmk-fingerpunch-vik (`config/west.yml`):**
- Replace `petejohanson/zmk@feat/pointers-move-scroll` → `zmkfirmware/zmk@v0.4`
- Same stale branch issue

**zmk-fingerpunch-vik (`config/deps.yml`):**
- Remove `cirque-input-module` project entry (now in upstream Zephyr)
- Verify `zmk-pmw3610-driver` compatibility (fork is 2 commits behind upstream `inorichi/zmk-pmw3610-driver`)

### 10. GitHub Actions Workflow Updates

All three repos' `.github/workflows/build.yml` must update from `@v0.3` / `@main` → `@v0.4`:
- `zmk-fingerpunch-keyboards/.github/workflows/build.yml`
- `zmk-fingerpunch-controllers/.github/workflows/build.yml`
- `zmk-fingerpunch-vik/.github/workflows/build.yml`

### 11. README Updates

- `zmk-fingerpunch-keyboards/README.md`: Remove stale references to `feat/pointers-move-scroll` branch; pointing devices are in ZMK since v0.2.0
- Update local build example command

### 12. Custom Board Names in build.yaml for Migrated Boards

After HWMv2 migration, custom fingerpunch boards will also need updated names in `build.yaml`. The exact format depends on whether you add a ZMK variant:
- `ffkb_holyiot_v1` → `ffkb_holyiot_v1//zmk` (if variant added)
- `vikoto` → `vikoto//zmk`
- `svlinky@0.2.0` → `svlinky@0.2.0//zmk`
- `xivik@0.3.0` → `xivik@0.3.0//zmk`
- etc.

**Note:** Whether to add ZMK variants to custom boards requires testing. If you don't add a `zmk` variant, the board name stays the same but you may miss ZMK-specific defaults.

### 13. Bootloader / Boot Retention Setup (ALL boards)

**What changed:** The previous method to enable `&bootloader` has been disabled. ZMK now requires "boot retention" to be configured for the bootloader reset behavior to work.

- All custom boards need boot retention set up per: https://zmk.dev/docs/development/hardware-integration/bootloader
- Without this, `&bootloader` behavior in keymaps will stop working
- Affects: `ffkb_holyiot_v1`, `vikoto`, `svlinky`, `xivik`, `pinkies_out_v3`, `vulpes_majora_v1`, `ximi`

### 14. Cirque `compatible` String Verification

**What changed:** The upstream Zephyr Cirque Pinnacle driver uses binding files named `cirque,pinnacle-i2c.yaml` and `cirque,pinnacle-spi.yaml`. The `compatible` string may remain `cirque,pinnacle` for both, but this **must be verified** at build time.

- Verify: Does `compatible = "cirque,pinnacle"` still work for both I2C and SPI in upstream Zephyr 4.1?
- If not, I2C overlays need `cirque,pinnacle-i2c` and SPI overlays need `cirque,pinnacle-spi`
- Also verify: the `sensitivity` property (used in `zmk-config-fp-example` `ffkb_v2.overlay`) — this may not exist in the upstream driver

### 15. Vikoto Charger Current Kconfig Preservation

**What changed:** The vikoto `Kconfig` file contains BOTH DC/DC settings (to be removed) AND charger current settings (`BOARD_VIKOTO_CHARGER_CURRENT_*`) that **must be preserved**. The charger current options are used by `pinmux.c` to configure GPIO pins for battery charging.

- Remove ONLY the `BOARD_ENABLE_DCDC` / `SOC_DCDC_NRF52X` sections from `Kconfig`
- **Keep** the `BOARD_VIKOTO_CHARGER_CURRENT` choice and all its options
- Verify `pinmux.c` still compiles — it uses `#if CONFIG_BOARD_VIKOTO` and `CONFIG_BOARD_VIKOTO_CHARGER_CURRENT_*` macros
- In HWMv2, `CONFIG_BOARD_VIKOTO` is still auto-set by the build system, so `pinmux.c` should work without changes
- The `CMakeLists.txt` (`zephyr_library_sources(pinmux.c)`) should also remain functional

### 16. Per-Board Overlay Naming for Custom FP Boards in VIK Shields

**What changed:** After HWMv2, boards move to `boards/<vendor>/<board>/`. The per-board overlay files in shield directories are matched by board name, NOT by directory path.

**Affected files in `zmk-fingerpunch-vik`:**
- `vik_cirque_i2c/boards/vikoto.overlay` — board name stays `vikoto`, should still match
- `vik_cirque_i2c/boards/sadekbaroudi_svlinky.overlay` — **⚠️ board name is `svlinky`, not `sadekbaroudi_svlinky`**. This file may need renaming to `svlinky.overlay`
- `vik_cirque_i2c/boards/sadekbaroudi_xivik.overlay` — **⚠️ same issue**, may need renaming to `xivik.overlay`

**Testing required:** Verify how Zephyr 4.1 resolves per-board overlays when the board is in a vendor directory. The `sadekbaroudi_` prefix convention may have been used to disambiguate boards from different modules — verify if this is still needed or if vendor directories handle it.

### 17. Seeeduino XIAO (SAMD21) Board Support

**What changed:** The ZMK blog documents renames for `seeeduino_xiao_ble` → `xiao_ble` and `seeeduino_xiao_rp2040` → `xiao_rp2040`, but does NOT mention the original `seeeduino_xiao` (SAMD21).

**Affected shields:** `arachnophobe` and `bykeeb` both have `boards/seeeduino_xiao.conf` files.

- Verify if the original XIAO SAMD21 is still supported in ZMK v0.4 / Zephyr 4.1
- If renamed, determine new name (likely `xiao` or similar)
- If dropped from ZMK, remove per-board overlays and update documentation
- This may affect users building for the original (non-BLE, non-RP2040) XIAO

### 18. zmk-config-fp-example and Downstream User Config Migration

**What changed:** The example repo and all user forks need updates for every breaking change above.

**Required updates to zmk-config-fp-example:**
- `config/west.yml`: Replace `petejohanson/zmk@feat/pointers-move-scroll` → `zmkfirmware/zmk@v0.4`, remove `petejohanson` remote
- `build.yaml`: `nice_nano_v2` → `nice_nano//zmk`
- `config/ffkb_v2.overlay`: Cirque property renames (`dr-gpios` → `data-ready-gpios`, remove `no-taps`, `sleep` → `sleep-mode-enable`). Verify `sensitivity` property compatibility. Verify `xy-swap`/`y-invert`/`x-invert` on `zmk,input-listener` are unchanged.
- `.github/workflows/build.yml`: Pin to `@v0.4` instead of `@main`
- `README.md`: Remove all references to `feat/pointers-move-scroll` and `cirque-input-module`; update instructions for v0.4 west.yml format and new board names

**Required user-facing migration guide** (see Phase 7):
- Users who DON'T use Cirque: only need west.yml + build.yaml + workflow updates
- Users who DO use Cirque: also need overlay property renames
- Users who reference `nice_nano_v2` or `seeeduino_xiao_ble`: need board name updates
- Users who reference custom FP boards: may need `//zmk` suffix if variants added

### 19. LVGL / Display Changes

**What changed:** LVGL was updated to 9.3.0 with breaking API changes.

- Verify: Do any fingerpunch boards/shields use LVGL displays or custom widgets?
- If so, update API calls per LVGL 9.3.0 changelog
- Currently no display usage identified, but verify to be safe

### 20. `board.cmake` File Updates

**What changed:** Board runner configurations in `board.cmake` may reference paths that have changed.

**Affected files:**
- `zmk-fingerpunch-controllers/boards/arm/vikoto/board.cmake`: References `${ZEPHYR_BASE}/boards/common/uf2.board.cmake` and `nrfjprog.board.cmake` — verify these paths still exist in Zephyr 4.1
- `zmk-fingerpunch-keyboards/boards/arm/ffkb_holyiot_v1/board.cmake`: Same concern
- `zmk-fingerpunch-keyboards/boards/arm/ximi/board.cmake`: Same concern

---

## Phased Implementation Checklist

### Phase 0: Preparation & Branch Setup
- [ ] Create a `feature/zmk-v0.4-migration` branch in all three repos
- [ ] Set up a local ZMK development environment with Zephyr 4.1 toolchain
- [ ] Run `west init` and `west update` against ZMK `main`
- [ ] Read the full Zephyr 4.1 migration blog post: https://zmk.dev/blog/2025/12/09/zephyr-4-1
- [ ] Read Zephyr HWMv2 porting guide: https://docs.zephyrproject.org/4.1.0/hardware/porting/board_porting.html

### Phase 1: zmk-fingerpunch-controllers Board Migration (dependency — must go first)
The keyboards repo depends on controllers via `config/deps.yml`, so controllers must be migrated first.

#### 1a. West manifest & workflow
- [ ] Update `config/west.yml`: change to `zmkfirmware/zmk` remote, `revision: main` (or v0.4 tag when available)
- [ ] Update `.github/workflows/build.yml`: change `@main` → `@v0.4` (or `@main` initially)

#### 1b. vikoto (nRF52840, non-revisioned)
- [ ] Move `boards/arm/vikoto/` → `boards/fingerpunch/vikoto/`
- [ ] Create `boards/fingerpunch/vikoto/board.yml`:
  ```yaml
  board:
    name: vikoto
    vendor: fingerpunch
    socs:
    - name: nrf52840
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.vikoto`
- [ ] In `Kconfig.vikoto`: Remove `bool` line, change `depends on SOC_NRF52840_QIAA` → `select SOC_NRF52840_QIAA`
- [ ] In `vikoto_defconfig`: Remove `CONFIG_SOC_SERIES_NRF52X=y`, `CONFIG_SOC_NRF52840_QIAA=y`, `CONFIG_BOARD_VIKOTO=y`
- [ ] In `Kconfig`: Remove ONLY `BOARD_ENABLE_DCDC` / `SOC_DCDC_NRF52X` section — **KEEP** `BOARD_VIKOTO_CHARGER_CURRENT` choice and all charger current options (used by `pinmux.c`)
- [ ] Remove DC/DC Kconfig from `Kconfig.defconfig` if present
- [ ] Add DC/DC devicetree node to `vikoto.dts`: `&reg1 { regulator-initial-mode = <NRF5X_REG_MODE_DCDC>; };`
- [ ] Set up boot retention for `&bootloader` behavior (see https://zmk.dev/docs/development/hardware-integration/bootloader)
- [ ] Verify `board.cmake` paths still valid in Zephyr 4.1 (`${ZEPHYR_BASE}/boards/common/uf2.board.cmake`, `nrfjprog.board.cmake`)
- [ ] Verify `pinmux.c` still compiles — `CONFIG_BOARD_VIKOTO` is auto-set by HWMv2 build system, and `CHARGER_CURRENT_*` options should still work
- [ ] Verify `CMakeLists.txt` (`zephyr_library_sources(pinmux.c)`) still functions
- [ ] Test build: `west build -b vikoto -- -DSHIELD=vik_vikropad`

#### 1c. svlinky (RP2040, revisioned: 0.1.0, 0.2.0)
- [ ] Move `boards/arm/svlinky/` → `boards/fingerpunch/svlinky/`
- [ ] Create `boards/fingerpunch/svlinky/board.yml`:
  ```yaml
  board:
    name: svlinky
    vendor: fingerpunch
    revision:
      format: major.minor.patch
      default: 0.2.0
      exact: true
      revisions:
      - name: 0.1.0
      - name: 0.2.0
    socs:
    - name: rp2040
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.svlinky`
- [ ] In `Kconfig.svlinky`: Remove `bool`, change `depends on` → `select SOC_RP2040`
- [ ] In `svlinky_defconfig`: Remove `CONFIG_SOC_SERIES_RP2XXX=y`, `CONFIG_BOARD_SVLINKY=y`
- [ ] Add `CONFIG_CLOCK_CONTROL=y` to `svlinky_defconfig`
- [ ] In `svlinky.dts`: Change `#include <rpi_pico/rp2040.dtsi>` → `#include <raspberrypi/rpi_pico/rp2040.dtsi>`
- [ ] In `svlinky.dts`: Remove `xtal-clk` / `fixed-clock` node
- [ ] In `svlinky.dts`: Add `&timer`, `&rtc`, `&vreg` nodes and `zephyr,flash-controller = &ssi;` to chosen
- [ ] Remove `revision.cmake` (revisions now handled by `board.yml`)
- [ ] Set up boot retention for `&bootloader` behavior
- [ ] Test build: `west build -b svlinky@0.2.0 -- -DSHIELD=vik_vikropad`

#### 1d. xivik (RP2040, revisioned: 0.1.0, 0.2.0, 0.3.0)
- [ ] Move `boards/arm/xivik/` → `boards/fingerpunch/xivik/`
- [ ] Create `boards/fingerpunch/xivik/board.yml`:
  ```yaml
  board:
    name: xivik
    vendor: fingerpunch
    revision:
      format: major.minor.patch
      default: 0.3.0
      exact: true
      revisions:
      - name: 0.1.0
      - name: 0.2.0
      - name: 0.3.0
    socs:
    - name: rp2040
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.xivik`
- [ ] In `Kconfig.xivik`: Remove `bool`, change `depends on` → `select SOC_RP2040`
- [ ] In `xivik_defconfig`: Remove `CONFIG_SOC_SERIES_RP2XXX=y`, `CONFIG_BOARD_XIVIK=y`
- [ ] Add `CONFIG_CLOCK_CONTROL=y` to `xivik_defconfig`
- [ ] In `xivik.dts`: Change `#include <rpi_pico/rp2040.dtsi>` → `#include <raspberrypi/rpi_pico/rp2040.dtsi>`
- [ ] In `xivik.dts`: Remove `xtal-clk` / `fixed-clock` node
- [ ] In `xivik.dts`: Add `&timer`, `&rtc`, `&vreg` nodes and `zephyr,flash-controller = &ssi;` to chosen
- [ ] Remove `revision.cmake`
- [ ] Set up boot retention for `&bootloader` behavior
- [ ] Test build: `west build -b xivik@0.3.0 -- -DSHIELD=arachnophobe`

#### 1e. Controllers validation
- [ ] Run full `build.yaml` matrix for controllers repo (if it has one)
- [ ] Push to feature branch, verify GitHub Actions pass

---

### Phase 2: zmk-fingerpunch-vik Migration
The keyboards repo depends on vik via `config/deps.yml`, so vik must be migrated before keyboards.

#### 2a. West manifest & workflow
- [ ] Update `config/west.yml`: change to `zmkfirmware/zmk` remote, `revision: main` (or v0.4)
- [ ] Update `.github/workflows/build.yml`: `@main` → `@v0.4`

#### 2b. Remove cirque-input-module dependency
- [ ] In `config/deps.yml`: Remove `cirque-input-module` project entry
- [ ] Verify `zmk-pmw3610-driver` still works with Zephyr 4.1 (may need updating or syncing with upstream `inorichi/zmk-pmw3610-driver`)

#### 2c. Migrate Cirque Pinnacle overlays to upstream Zephyr driver
- [ ] `vik_cirque_i2c/vik_cirque_i2c.overlay`:
  - [ ] Change `compatible = "cirque,pinnacle"` → verify upstream compatible string (`cirque,pinnacle-i2c` for I2C)
  - [ ] Rename `dr-gpios` → `data-ready-gpios`
  - [ ] Remove `no-taps` (add `primary-tap-enable` if taps are desired)
  - [ ] Rename `x-invert`/`y-invert` → `invert-x`/`invert-y` (if present)
  - [ ] Rename `rotate-90` → `swap-xy` (if present)
  - [ ] Rename `sleep` → `sleep-mode-enable` (if present)
- [ ] `vik_cirque_spi/vik_cirque_spi.overlay`: Same property renames as above (compatible may be `cirque,pinnacle-spi`)
- [ ] `vik_cirque_spi_split/vik_cirque_spi_split_peripheral.overlay`: Same property renames
- [ ] Update any `.conf` files if Kconfig options changed

#### 2d. Rename per-board overlay files in vik shields
- [ ] `vik_cirque_i2c/boards/vikoto.overlay` — board name stays `vikoto`, verify still matches after HWMv2
- [ ] `vik_cirque_i2c/boards/sadekbaroudi_svlinky.overlay` — **⚠️ board name is `svlinky`**, likely needs rename to `svlinky.overlay` (the `sadekbaroudi_` prefix was a module disambiguation convention that may no longer apply with vendor directories)
- [ ] `vik_cirque_i2c/boards/sadekbaroudi_xivik.overlay` — **⚠️ same issue**, likely needs rename to `xivik.overlay`
- [ ] Test each renamed overlay actually gets picked up during build

#### 2e. Vik validation
- [ ] Test build with a cirque shield against a migrated controller board
- [ ] Push to feature branch, verify GitHub Actions pass

---

### Phase 3: zmk-fingerpunch-keyboards Board Migration

#### 3a. West manifest & workflow
- [ ] Update `config/west.yml`: change `revision: v0.3` → `revision: main` (or v0.4)
- [ ] Update `.github/workflows/build.yml`: `@v0.3` → `@v0.4`
- [ ] Verify `chiptuner-zmk-module` compatibility with Zephyr 4.1

#### 3b. ffkb_holyiot_v1 (nRF52840, non-revisioned, non-split)
- [ ] Move `boards/arm/ffkb_holyiot_v1/` → `boards/fingerpunch/ffkb_holyiot_v1/`
- [ ] Create `boards/fingerpunch/ffkb_holyiot_v1/board.yml`:
  ```yaml
  board:
    name: ffkb_holyiot_v1
    vendor: fingerpunch
    socs:
    - name: nrf52840
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.ffkb_holyiot_v1`
- [ ] In `Kconfig.ffkb_holyiot_v1`: Remove `bool`, change `depends on SOC_NRF52840_QIAA` → `select SOC_NRF52840_QIAA`
- [ ] In `ffkb_holyiot_v1_defconfig`: Remove `CONFIG_SOC_SERIES_NRF52X=y`, `CONFIG_SOC_NRF52840_QIAA=y`
- [ ] Remove DC/DC Kconfig from `Kconfig` (`BOARD_ENABLE_DCDC` / `SOC_DCDC_NRF52X`)
- [ ] Remove DC/DC Kconfig from `Kconfig.defconfig` if present
- [ ] Add DC/DC devicetree node to `ffkb_holyiot_v1.dts`: `&reg1 { regulator-initial-mode = <NRF5X_REG_MODE_DCDC>; };`
- [ ] Remove `CONFIG_WS2812_STRIP=y` from `ffkb_holyiot_v1_defconfig`
- [ ] Verify `board.cmake` paths still valid in Zephyr 4.1
- [ ] Set up boot retention for `&bootloader` behavior
- [ ] Test build: `west build -b ffkb_holyiot_v1`

#### 3c. pinkies_out_v3 (RP2040, revisioned: 3.0.0, 3.1.0)
- [ ] Move `boards/arm/pinkies_out_v3/` → `boards/fingerpunch/pinkies_out_v3/`
- [ ] Create `boards/fingerpunch/pinkies_out_v3/board.yml`:
  ```yaml
  board:
    name: pinkies_out_v3
    vendor: fingerpunch
    revision:
      format: major.minor.patch
      default: 3.1.0
      exact: true
      revisions:
      - name: 3.0.0
      - name: 3.1.0
    socs:
    - name: rp2040
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.pinkies_out_v3`
- [ ] In `Kconfig.pinkies_out_v3`: Remove `bool`, change `depends on` → `select SOC_RP2040`
- [ ] In `pinkies_out_v3_defconfig`: Remove `CONFIG_SOC_SERIES_RP2XXX=y`, `CONFIG_BOARD_PINKIES_OUT_V3=y`
- [ ] Add `CONFIG_CLOCK_CONTROL=y` to `pinkies_out_v3_defconfig`
- [ ] Remove `CONFIG_WS2812_STRIP=y` from `pinkies_out_v3_defconfig`
- [ ] In `pinkies_out_v3.dts`: Change RP2040 include path
- [ ] In `pinkies_out_v3.dts`: Remove `fixed-clock` node
- [ ] In `pinkies_out_v3.dts`: Add `&timer`, `&rtc`, `&vreg`, flash-controller nodes
- [ ] Remove `revision.cmake`
- [ ] Set up boot retention for `&bootloader` behavior
- [ ] Test build: `west build -b pinkies_out_v3@3.1.0 -- -DSHIELD=vik_cirque_spi`

#### 3d. vulpes_majora_v1 (RP2040, non-revisioned)
- [ ] Move `boards/arm/vulpes_majora_v1/` → `boards/fingerpunch/vulpes_majora_v1/`
- [ ] Create `boards/fingerpunch/vulpes_majora_v1/board.yml`:
  ```yaml
  board:
    name: vulpes_majora_v1
    vendor: fingerpunch
    socs:
    - name: rp2040
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.vulpes_majora_v1`
- [ ] In `Kconfig.vulpes_majora_v1`: Remove `bool`, change `depends on` → `select SOC_RP2040`
- [ ] In `vulpes_majora_v1_defconfig`: Remove `CONFIG_SOC_SERIES_RP2XXX=y`, `CONFIG_BOARD_VULPES_MAJORA_V1=y`
- [ ] Add `CONFIG_CLOCK_CONTROL=y` to `vulpes_majora_v1_defconfig`
- [ ] Remove `CONFIG_WS2812_STRIP=y` from `vulpes_majora_v1_defconfig`
- [ ] In `vulpes_majora_v1.dts`: Change RP2040 include path
- [ ] In `vulpes_majora_v1.dts`: Remove `fixed-clock` node
- [ ] In `vulpes_majora_v1.dts`: Add `&timer`, `&rtc`, `&vreg`, flash-controller nodes
- [ ] Set up boot retention for `&bootloader` behavior
- [ ] Test build: `west build -b vulpes_majora_v1 -- -DSHIELD=vik_pmw3360_per56`

#### 3e. ximi (RP2040, revisioned: 1.0.0, 2.0.0, SPLIT — manual migration)
- [ ] Move `boards/arm/ximi/` → `boards/fingerpunch/ximi/`
- [ ] Create `boards/fingerpunch/ximi/board.yml`:
  ```yaml
  boards:
  - name: ximi_left
    vendor: fingerpunch
    revision:
      format: major.minor.patch
      default: 2.0.0
      exact: true
      revisions:
      - name: 1.0.0
      - name: 2.0.0
    socs:
    - name: rp2040
  - name: ximi_right
    vendor: fingerpunch
    revision:
      format: major.minor.patch
      default: 2.0.0
      exact: true
      revisions:
      - name: 1.0.0
      - name: 2.0.0
    socs:
    - name: rp2040
  ```
- [ ] Rename `Kconfig.board` → `Kconfig.ximi`
- [ ] In `Kconfig.ximi`: Remove `bool` lines, change `depends on` → `select SOC_RP2040` for both BOARD_XIMI_LEFT and BOARD_XIMI_RIGHT
- [ ] In `ximi_left_defconfig`: Remove `CONFIG_SOC_SERIES_RP2XXX=y`, `CONFIG_BOARD_XIMI_LEFT=y`
- [ ] In `ximi_right_defconfig`: Remove `CONFIG_SOC_SERIES_RP2XXX=y`, `CONFIG_BOARD_XIMI_RIGHT=y`
- [ ] Add `CONFIG_CLOCK_CONTROL=y` to both defconfigs
- [ ] In `ximi.dtsi`: Change RP2040 include path
- [ ] In `ximi.dtsi`: Remove `fixed-clock` node
- [ ] In `ximi.dtsi`: Add `&timer`, `&rtc`, `&vreg`, flash-controller nodes
- [ ] Remove `revision.cmake`
- [ ] Verify `board.cmake` paths still valid in Zephyr 4.1
- [ ] Set up boot retention for `&bootloader` behavior
- [ ] Test build: `west build -b ximi_left@2.0.0 -- -DSHIELD=vik_cirque_spi`
- [ ] Test build: `west build -b ximi_right@2.0.0`

---

### Phase 4: Shield & Overlay Updates (zmk-fingerpunch-keyboards)

#### 4a. Cirque Pinnacle property renames in shields
- [ ] Verify `compatible = "cirque,pinnacle"` is correct for upstream Zephyr 4.1 driver (may need `cirque,pinnacle-i2c` or `cirque,pinnacle-spi`)
- [ ] Verify `sensitivity` property (used in zmk-config-fp-example) is supported by upstream driver
- [ ] `ffkb_v2/ffkb_v2.overlay`: Rename `dr-gpios` → `data-ready-gpios`, remove `no-taps`
- [ ] `ffkb_we_v1/ffkb_we_v1.overlay`: Rename `dr-gpios` → `data-ready-gpios`, remove `no-taps`
- [ ] `pinkies_out_v2/pinkies_out_v2.overlay`: Rename `dr-gpios` → `data-ready-gpios`, remove `no-taps`

#### 4b. CONFIG_WS2812_STRIP cleanup in shield .conf files
- [ ] Remove any remaining `CONFIG_WS2812_STRIP=y` from `.conf` files (most are already commented out)

#### 4c. Verify CONFIG_ZMK_BLE_EXPERIMENTAL_* options
- [ ] Check if `CONFIG_ZMK_BLE_EXPERIMENTAL_CONN` and `CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES` still exist in ZMK main Kconfig
- [ ] If removed, clean up from all `.conf` files (15+ files)
- [ ] If deprecated with replacement, update accordingly

#### 4d. Rename per-board overlay files for renamed ZMK in-tree boards
- [ ] `ffkb_v1/boards/nice_nano_v2.overlay` → `nice_nano.overlay` (merge with or replace existing `nice_nano.overlay` for v1)
- [ ] `ffkb_v1/boards/nice_nano_v2.conf` → `nice_nano.conf`
- [ ] `ffkb_v1/boards/nice_nano_v2.keymap` → `nice_nano.keymap`
- [ ] `ffkb_v1/boards/nice_nano.overlay` → `nice_nano@1.overlay` (if v1 support still needed)
- [ ] `ffkb_v2/boards/nice_nano_v2.overlay` → `nice_nano.overlay`
- [ ] `ffkb_v2/boards/nice_nano_v2.conf` → `nice_nano.conf`
- [ ] `ffkb_v2/boards/nice_nano_v2.keymap` → `nice_nano.keymap`
- [ ] `ffkb_v2/boards/nice_nano.overlay` → `nice_nano@1.overlay`
- [ ] `ffkb_v3/boards/nice_nano_v2.overlay` → `nice_nano.overlay`
- [ ] `pinkies_out_v1/boards/nice_nano_v2.overlay` → `nice_nano.overlay`
- [ ] `pinkies_out_v1/boards/nice_nano.overlay` → `nice_nano@1.overlay`
- [ ] `pinkies_out_v2/boards/nice_nano_v2.overlay` → `nice_nano.overlay`
- [ ] `pinkies_out_v2/boards/nice_nano_v2.keymap` → `nice_nano.keymap`
- [ ] `rock_on_v1/boards/nice_nano_v2.overlay` → `nice_nano.overlay`
- [ ] `rock_on_v1/boards/nice_nano.overlay` → `nice_nano@1.overlay`
- [ ] `vulpes_minora/boards/nice_nano_v2.overlay` → `nice_nano.overlay`
- [ ] `vulpes_minora/boards/nice_nano_v2.conf` → `nice_nano.conf`
- [ ] `vulpes_minora/boards/nice_nano_v2.keymap` → `nice_nano.keymap`
- [ ] `vulpes_minora/boards/nice_nano.overlay` → `nice_nano@1.overlay`
- [ ] `arachnophobe/boards/seeeduino_xiao_ble.conf` → `xiao_ble.conf`
- [ ] `arachnophobe/boards/seeeduino_xiao_rp2040.conf` → `xiao_rp2040.conf`
- [ ] `arachnophobe/boards/seeeduino_xiao.conf` → **verify SAMD21 XIAO support in ZMK v0.4**: if renamed, update filename; if dropped, remove file and document for users
- [ ] `bykeeb/boards/seeeduino_xiao_ble.conf` → `xiao_ble.conf`
- [ ] `bykeeb/boards/seeeduino_xiao_rp2040.conf` → `xiao_rp2040.conf`
- [ ] `bykeeb/boards/seeeduino_xiao.conf` → same SAMD21 verification as arachnophobe above

---

### Phase 5: build.yaml Updates (zmk-fingerpunch-keyboards)

#### 5a. Update ZMK in-tree board names
- [ ] Replace all `seeeduino_xiao_ble` → `xiao_ble//zmk` in build.yaml
- [ ] Replace all `seeeduino_xiao_rp2040` → `xiao_rp2040//zmk` in build.yaml (if any)
- [ ] Replace all `nice_nano_v2` → `nice_nano//zmk` in build.yaml
- [ ] Replace all `nice_nano` (v1, if any) → `nice_nano@1//zmk` in build.yaml
- [ ] Replace `settings_reset` board entry if renamed in ZMK v0.4

#### 5b. Update custom fingerpunch board names (if ZMK variants added)
- [ ] Determine if custom boards need `//zmk` variant suffix
- [ ] If yes, update all `ffkb_holyiot_v1`, `pinkies_out_v3@*`, `vulpes_majora_v1`, `ximi_left@*`, `ximi_right@*` entries
- [ ] Update all `vikoto`, `svlinky@*`, `xivik@*` entries

#### 5c. Verify all build combinations
- [ ] Run a subset of builds locally to verify
- [ ] Push and verify full GitHub Actions matrix passes

---

### Phase 6: Documentation & Cleanup

#### 6a. README updates
- [ ] `zmk-fingerpunch-keyboards/README.md`: Remove references to `feat/pointers-move-scroll`
- [ ] Update local build example command with new board name format
- [ ] Mention pinned version and how to update

#### 6b. zmk-fingerpunch-controllers README
- [ ] Update if it references the stale pointers branch

#### 6c. zmk-pmw3610-driver
- [ ] Sync fork with upstream `inorichi/zmk-pmw3610-driver` (2 commits behind)
- [ ] Verify compatibility with Zephyr 4.1

#### 6d. Final validation
- [ ] Run full build matrix across all three repos
- [ ] Flash and test on at least one physical board per SoC type (nRF52840 + RP2040)
- [ ] Verify BLE connectivity, pointing devices, encoders, RGB
- [ ] Verify `&bootloader` behavior works on at least one board (boot retention)
- [ ] Tag releases in all three repos
- [ ] Merge feature branches to main

---

### Phase 7: zmk-config-fp-example Migration & User Communication

This phase is **critical** because hundreds of users have forked or followed this template. It must happen AFTER all upstream repos are migrated and tested (Phases 1-6), but the communication plan should be drafted during Phase 0.

#### 7a. Update zmk-config-fp-example repo
- [ ] `config/west.yml`:
  - [ ] Replace `petejohanson` remote + `feat/pointers-move-scroll` → `zmkfirmware/zmk` remote with `revision: v0.4`
  - [ ] Remove `petejohanson` remote entirely
  - [ ] Keep `sadekbaroudi` remote and `zmk-fingerpunch-keyboards` import
- [ ] `build.yaml`:
  - [ ] Replace `nice_nano_v2` → `nice_nano//zmk`
- [ ] `config/ffkb_v2.overlay`:
  - [ ] Rename `dr-gpios` → `data-ready-gpios`
  - [ ] Remove `no-taps` (it was opt-out; upstream uses opt-in `primary-tap-enable`)
  - [ ] Rename `sleep` → `sleep-mode-enable`
  - [ ] Verify `sensitivity = "4x"` is supported by upstream Cirque driver (remove if not)
  - [ ] Verify `compatible = "cirque,pinnacle"` is correct for I2C usage
  - [ ] Verify `zmk,input-listener` properties (`xy-swap`, `y-invert`, `x-invert`) are unchanged in ZMK v0.4
- [ ] `.github/workflows/build.yml`:
  - [ ] Change `@main` → `@v0.4` to pin to stable release
- [ ] `README.md`:
  - [ ] Remove all references to `feat/pointers-move-scroll` branch
  - [ ] Remove references to `cirque-input-module` (now upstream in Zephyr)
  - [ ] Update `config/west.yml` instructions to show v0.4 format
  - [ ] Update board name examples to use new format (`nice_nano//zmk`, `xiao_ble//zmk`)
  - [ ] Add note about pointing devices being in ZMK since v0.2.0
  - [ ] Add link to user migration guide
- [ ] Test build: verify the example repo builds successfully end-to-end

#### 7b. Write user-facing migration guide
Create a migration guide (e.g., `MIGRATING_TO_V0.4.md` or a GitHub Discussion/Wiki page) covering:

- [ ] **Who needs to do what** — triage by use case:
  - Users with NO Cirque and NO custom overlays: only update `west.yml` + `build.yaml` + workflow
  - Users with Cirque trackpad (direct wiring): update `west.yml` + `build.yaml` + Cirque overlay properties
  - Users with VIK Cirque modules: only update `west.yml` + `build.yaml` (VIK shield handles driver migration)
  - Users referencing custom FP boards: update board names if `//zmk` variant added

- [ ] **Step-by-step west.yml migration:**
  ```yaml
  # OLD (v0.3 / feat/pointers-move-scroll):
  projects:
    - name: zmk
      remote: petejohanson
      revision: feat/pointers-move-scroll
      import: app/west.yml

  # NEW (v0.4):
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v0.4
      import: app/west.yml
  ```

- [ ] **Step-by-step build.yaml migration:**
  | Old Board Name | New Board Name |
  |---|---|
  | `nice_nano_v2` | `nice_nano//zmk` |
  | `nice_nano` (v1) | `nice_nano@1//zmk` |
  | `seeeduino_xiao_ble` | `xiao_ble//zmk` |
  | `seeeduino_xiao_rp2040` | `xiao_rp2040//zmk` |

- [ ] **Step-by-step Cirque overlay migration** (for users with direct Cirque wiring):
  | Old Property | New Property |
  |---|---|
  | `dr-gpios` | `data-ready-gpios` |
  | `no-taps` | *(remove entirely; add `primary-tap-enable` if you WANT taps)* |
  | `sleep` | `sleep-mode-enable` |
  | `x-invert` / `y-invert` | `invert-x` / `invert-y` *(on Cirque device node only; `zmk,input-listener` properties are unchanged)* |
  | `rotate-90` | `swap-xy` |

- [ ] **GitHub Actions workflow update:** Change `@main` or `@v0.3` → `@v0.4`

- [ ] **Common errors and solutions** — document expected error messages users will see if they partially migrate

#### 7c. Communication plan
- [ ] Draft GitHub release notes for all four repos
- [ ] Post announcement to Discord / community channels
- [ ] Consider opening GitHub Issues in `zmk-config-fp-example` with migration instructions
- [ ] Pin a GitHub Discussion or Wiki page with the migration guide
- [ ] Add a deprecation notice / migration banner to README.md on the v0.3 branch before merging

#### 7d. Coordinated release timing
- [ ] All four repos must be released together (or in dependency order: controllers → vik → keyboards → example)
- [ ] If using feature branches: merge controllers first, then vik, then keyboards, then example
- [ ] `zmk-fingerpunch-keyboards/config/deps.yml` and `zmk-fingerpunch-controllers/config/deps.yml` point to `main` branches of dependencies — ensure all dependency repos are merged to `main` before the keyboards repo
- [ ] Consider tagging all repos with matching version tags (e.g., `v0.4-compat` or similar)
- [ ] Verify the full dependency chain works end-to-end: user config → keyboards → controllers → vik → zmk v0.4

---

## Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| `zmk-pmw3610-driver` incompatible with Zephyr 4.1 | Medium | High — breaks all PMW3610 builds | Test early; upstream may have fixes |
| `chiptuner-zmk-module` incompatible with Zephyr 4.1 | Medium | Low — only affects chiptuner builds | Can be removed if abandoned |
| Per-board overlay rename causes shield matching failures | High | Medium | Test each shield/board combo |
| Split board (ximi) manual migration introduces bugs | Medium | High — split builds are complex | Test both halves independently and together |
| `CONFIG_ZMK_BLE_EXPERIMENTAL_*` removed in v0.4 | Low | Medium | Check Kconfig before migration |
| LVGL API changes break custom display code | Low | Low — no custom widgets identified | Verify at build time |
| VIK per-board overlay naming (`sadekbaroudi_*` prefix) breaks after HWMv2 | Medium | High — breaks VIK Cirque on svlinky/xivik | Test naming convention early in Phase 2 |
| Cirque `compatible` string changes from `cirque,pinnacle` to `cirque,pinnacle-i2c`/`-spi` | Low | High — breaks ALL Cirque builds | Verify against Zephyr 4.1 source early |
| Cirque `sensitivity` property not in upstream driver | Medium | Low — only affects users who set it | Test and document; remove if unsupported |
| Boot retention misconfiguration breaks `&bootloader` on all boards | Medium | Medium — affects firmware update UX | Follow ZMK docs carefully; test on physical hardware |
| `board.cmake` runner paths changed in Zephyr 4.1 | Low | Low — only affects JTAG/SWD flashing | Verify paths exist |
| Users don't see migration guide and builds break | High | High — hundreds of users affected | Proactive communication; clear error messages in README |
| Partial migration (some repos updated, others not) causes dependency conflicts | Medium | High — cascading build failures | Enforce dependency order; document clearly |
| SAMD21 XIAO support dropped in ZMK v0.4 | Medium | Low — few users on original XIAO | Verify and document; remove per-board overlays if needed |

## Estimated Scope

- **7 board definitions** to migrate (4 keyboards + 3 controllers)
- **7 Cirque overlay files** to update (3 keyboards + 3 vik + 1 example)
- **~25 per-board overlay files** to rename in shields
- **~2-3 VIK per-board overlays** to rename (`sadekbaroudi_*` prefix)
- **~60 build.yaml entries** to update board names (keyboards) + 1 (example)
- **4 west.yml files** + **4 workflow files** to update (3 upstream repos + example)
- **3 deps.yml files** to clean up
- **7 boards** need boot retention setup
- **1 user migration guide** to write
- **1 README** major rewrite (zmk-config-fp-example)
